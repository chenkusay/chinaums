<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.fhl.dao.GoodsMemberUpgradesDao">

    <sql id="Base_Column_List">
		            a.`id` AS "id", 
		            a.`name` AS "name", 
		            a.`goods_sn` AS "goodsSn", 
		            a.`master_pics` AS "masterPics", 
		            a.`brief` AS "brief", 
		            a.`seller_note` AS "sellerNote", 
		            a.`content` AS "content", 
		            a.`price` AS "price", 
		            a.`discount_point` AS "discountPoint", 
		            a.`start_time` AS "startTime", 
		            a.`end_time` AS "endTime", 
		            a.`sales_num` AS "salesNum", 
		            a.`sort` AS "sort", 
		            a.`create_time` AS "createTime", 
		            a.`update_time` AS "updateTime"
		    </sql>

    <select id="getList" resultType="GoodsMemberUpgrades">
        select
          <include refid="Base_Column_List"/>
        from goods_member_upgrades a
        <where>
                            <if test="id != null">
                    and a.`id` = #{id}
                </if>
                            <if test="name != null">
                    and a.`name` = #{name}
                </if>
                            <if test="goodsSn != null">
                    and a.`goods_sn` = #{goodsSn}
                </if>
                            <if test="masterPics != null">
                    and a.`master_pics` = #{masterPics}
                </if>
                            <if test="brief != null">
                    and a.`brief` = #{brief}
                </if>
                            <if test="sellerNote != null">
                    and a.`seller_note` = #{sellerNote}
                </if>
                            <if test="content != null">
                    and a.`content` = #{content}
                </if>
                            <if test="price != null">
                    and a.`price` = #{price}
                </if>
                            <if test="discountPoint != null">
                    and a.`discount_point` = #{discountPoint}
                </if>
                            <if test="startTime != null">
                    and a.`start_time` = #{startTime}
                </if>
                            <if test="endTime != null">
                    and a.`end_time` = #{endTime}
                </if>
                            <if test="salesNum != null">
                    and a.`sales_num` = #{salesNum}
                </if>
                            <if test="sort != null">
                    and a.`sort` = #{sort}
                </if>
                            <if test="createTime != null">
                    and a.`create_time` = #{createTime}
                </if>
                            <if test="updateTime != null">
                    and a.`update_time` = #{updateTime}
                </if>
                    </where>
    </select>

    <!-- 级联删除 -->
    <delete id="deleteBatch">
        delete from goods_member_upgrades where find_in_set(id, #{ids});
    </delete>

</mapper>